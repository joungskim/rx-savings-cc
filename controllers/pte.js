/* const { getNearest } = require('./pharmacyController');
const { deleteOne } = require('../model/Pharmacy'); */

/* describe('getNearest', () => {
    it('Should fail validation.', () => {
        try {
            expect(getNearest({ latitude: "string", longitude: 31 }).toBe(`"latitude" must be a number`));
            done();
        } catch (err) {
            done(error);
        }
    })
}); */